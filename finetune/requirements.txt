# Fine-tuning dependencies for the GPU Qwen 3.5 + Unsloth pipeline.
# Install CUDA-enabled PyTorch first, for example:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision torchaudio

torch>=2.11,<2.12
unsloth>=2026.3.17
transformers>=5.3,<5.4
trl==0.22.2
datasets>=2.21,<4
accelerate>=1.10,<2
peft>=0.18.0
bitsandbytes>=0.47,<0.48
safetensors>=0.5,<1
huggingface_hub[hf_transfer]>=0.34,<1
sentencepiece>=0.2
tensorboard>=2.18,<3
pytest>=9,<10
